---
version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
  exclusions:
    rules:
      - linters:
          - revive
        text: "^exported:"

formatters:
  enable:
    - gofmt
    - gofumpt
